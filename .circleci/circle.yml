version: 2
jobs:
  build:
    
    working_directory: /home/dellpc/shahul/learning/circleci/sampleapp
    java:
    version: openjdk8

    steps:
      - run: echo '$PATH'
      - checkout

      - restore_cache:
          key: sampleapp-{{ checksum "pom.xml" }}
      
      - run: 
          name: post checkout check
          command: |
            echo $PWD
      - save_cache:
          paths:
            - ~/.m2
          key: sampleapp-{{ checksum "pom.xml" }}
      
      - run: mvn clean install
      
      - store_test_results:
          path: /home/dellpc/shahul/learning/circleci/sampleapp/target/surefire-reports
      
      - store_artifacts:
          path: /home/dellpc/shahul/learning/circleci/sampleapp/target/sampleapp.jar    